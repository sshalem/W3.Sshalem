import { MainChildArea } from "../../../../../components";

const EurekaIntro = ({ anchor }: { anchor: string }) => {
  return <MainChildArea anchor={anchor}>Eureka Intro</MainChildArea>;
};

export default EurekaIntro;
