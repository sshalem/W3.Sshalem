import { MainChildArea } from "../../../../../components";

const O4_FirewallConfig = ({ anchor }: { anchor: string }) => {
  return <MainChildArea anchor={anchor}>config firewall</MainChildArea>;
};
export default O4_FirewallConfig;
