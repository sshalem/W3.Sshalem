import { MainChildArea } from "../../../../components";

const DeployTomcatMain = ({ anchor }: { anchor: string }) => {
  return <MainChildArea anchor={anchor}>Add any content here</MainChildArea>;
};
export default DeployTomcatMain;
