import { MainChildArea } from "../../../../../components";

const O1_Intro = ({ anchor }: { anchor: string }) => {
  return <MainChildArea anchor={anchor}>Stream Large JSON response</MainChildArea>;
};

export default O1_Intro;
