// I add '/' for each link
// Why?
// because location.pathname gives result as follows "/spring" , adding the forward slash
// Thus, I add forward slash to each link

export const links: string[] = ["/spring", "/micro-services", "/reactJS", "/javascript", "/html_css", "/fullstack", "/java", "/sql", "/python", "/devops", "/git"];
