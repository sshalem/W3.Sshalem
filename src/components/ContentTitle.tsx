const ContentTitle = ({ anchor }: { anchor: string }) => {
  return <div className="mb-8 inline-block bg-gray-800 px-2 py-2 font-semibold capitalize tracking-wider text-white">{anchor}</div>;
};

export default ContentTitle;
