import { MainChildArea } from "../../../components";

const EurekaDiscoveryIntro = ({ anchor }: { anchor: string }) => {
  return <MainChildArea anchor={anchor}>Eureka Discovery Intro ...</MainChildArea>;
};

export default EurekaDiscoveryIntro;
